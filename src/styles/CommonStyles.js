import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
    screenRoot: {
        flex: 1,
    },
    mainTabImg: {
        width: 24,
        height: 24,
    }
});

